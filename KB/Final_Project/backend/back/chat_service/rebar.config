{erl_opts, [debug_info]}.

{deps, [
    % WebSocket 서버 (Cowboy)
    {cowboy, {git, "https://github.com/ninenines/cowboy.git", {tag, "2.13.0"}}},
    
    % JWT 토큰 처리
    {jwt, ".*", {git, "https://github.com/artemeff/jwt", {tag, "0.1.11"}}},
    
    % JSON 처리
    {jsx, "3.1.0"},
    
    % Scylla DB 연결
    {cqerl, {git, "https://github.com/cqerl/cqerl", {tag, "v1.1.3"}}},

    % Redis 연결  
    {eredis, "1.7.1"}
]}.

{shell, [
    %% {config, "config/sys.config"},
    {apps, [chat_service]}
]}.
